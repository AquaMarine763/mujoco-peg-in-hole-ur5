# v201 diagnostic v200 with high-Z-only direct finish boost.
#
# v200 fixes the v198 929500 timeout bucket but introduces a low-Z collision
# in 931510 because the direct fast-settle down boost remains active around
# 27-30 mm above target. This keeps the direct handoff and high-Z catch-up
# behavior but disables the extra down speed once the peg reaches 34 mm above
# target, handing low-Z insertion back to the existing conservative v198 stack.
#
# Diagnostic only until 929500/931500 and the focused hard-square gate pass.
base_config: eval_multi_geometry_v200_risk_only_nolift_brake4_down10_late_boost_prepop_relief_direct_finish_hard_square_30ep.yaml

guard_final_servo_square_recovery_escape_direct_fast_settle_max_down_z_min: 0.034
